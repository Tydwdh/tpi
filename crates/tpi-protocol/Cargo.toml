[package]
name = "tpi-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "TPI multi-frontend protocol: ClientCommand / RuntimeEvent / View DTO / Structured Error / Envelope"

[dependencies]
tpi-core = { path = "../tpi-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
