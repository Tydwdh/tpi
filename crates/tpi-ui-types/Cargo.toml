[package]
name = "tpi-ui-types"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "TPI UI data types shared by config and TUI (ViewMode, Keymap, KeyAction)"

[dependencies]
crossterm = "0.29"
toml = "1"
tracing = "0.1"
